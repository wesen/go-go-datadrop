# Tasks

## TODO

- [x] 1. pkg/tabular: Table/Field types, dotted-path flattening moved out of export, CSV/NDJSON/JSON readers moved out of import, typed inference with provenance (guide §6, §7) <!-- t:ta6w -->
- [x] 2. GET /v1/drops/{name}/streams — stream catalogue from stream_heads (guide §9.1) <!-- t:on88 -->
- [x] 3. GET /v1/drops/{name}/table — event stream projected to a typed table (guide §9.2) <!-- t:a3sj -->
- [x] 4. GET .../datasets/{dataset}/versions/{version}/table — dataset file projected to a typed table (guide §9.3) <!-- t:m1ql -->
- [x] 5. pkg/webui: embed + disk FS, SPA handler at /ui, assets at /static, go:generate build (guide §10) <!-- t:9qpb -->
- [x] 6. Frontend scaffold: vite/bun/react/ts/rtk-query/bootstrap, token bar, source picker on live endpoints (guide §11) <!-- t:5int -->
- [x] 7. Frontend model: table types, pipeline engine, chart spec, plot engine — pure TS with bun tests (guide §12) <!-- t:wc14 -->
- [x] 8. Frontend UI: pipeline editor, encoding editor, PlotSvg with facets and legend, data table (guide §13) <!-- t:ntxd -->
- [x] 9. Live tail: SSE-backed stream charts with an explicit gap indicator (guide §14) <!-- t:2jw4 -->
- [x] 10. Export and permalink: chart to PNG, table to CSV, spec in the URL hash (guide §15) <!-- t:zqco -->
- [x] 11. Tests and docs: tabular/handler tests, bun tests, end-to-end smoke, Playwright render check, README (guide §16) <!-- t:9qaa -->
