# Tasks

## TODO

- [ ] Phase 1 — Stages: Stage/StageChrome, stageId on Workspace, the mirrored space pointer, spaces.ts becomes stages.ts with four pinned stages, persisted version 1 to 2 with a migration, StageBar in the masthead, the signed-out gate sets a stage <!-- t:pw1p -->
- [ ] Phase 2 — Tile identity and multiplicity: leaf label and renameLeaf, duplicateLeaf, duplicable and singleton on all 25 descriptors, SelectOption.disabled with a reason, double-click renames <!-- t:nve4 -->
- [ ] Phase 3 — The portable format: model/portable.ts, bundleForTile/Workspace/Stage and their inverses, parseBundle with every reject reason, the caps, and portable.test.ts <!-- t:x3cy -->
- [ ] Phase 4 — The verb seam widened: actionsForVerb over whole state returning thunks, applyLayoutVerb.ts, ClipboardPort on the thunk extra argument, effects.ts, TileRef/WorkspaceRef/StageRef and the three descriptors, pendingImport with save() enumerating layout fields <!-- t:416y -->
- [ ] Phase 5 — Export and import in the interface: TextArea atom, Dialog organism, BundleDialog, clipboard prefill only on a successful parse, live describeBundle, and a Firefox verification of the empty path <!-- t:2h2c -->
- [ ] Phase 6 — Templates: store/templates.ts with the three caps, TemplateTable, TemplatesApp on the account stage, store/load/rename/delete/copy, delete confirms <!-- t:recw -->
- [ ] Phase 7 — Guards, stories and the tour: stories for the seven new components, tour seeds gain a stage, the module rack gains the templates card, verify every test by breaking it <!-- t:jyz1 -->
